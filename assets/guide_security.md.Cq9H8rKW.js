import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.DiAOIirN.js";var i=JSON.parse(`{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"guide/security.md","filePath":"guide/security.md","lastUpdated":1788526325000}`),a={name:`guide/security.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h1><p>This page is the prose version of <a href="https://github.com/ni-c/imap-mcp/blob/main/SECURITY.md" target="_blank" rel="noreferrer">SECURITY.md</a>.</p><h2 id="trust-model" tabindex="-1">Trust model <a class="header-anchor" href="#trust-model" aria-label="Permalink to “Trust model”">​</a></h2><p><strong>Everything this server returns from the mailbox is attacker-controlled.</strong> Senders, subjects, bodies, folder names, attachment filenames — anyone in the world can send mail, so all of it is evidence to report on, never instruction to follow.</p><p>The server says so three times over, because one place is not enough: in the <code>instructions</code> field of the MCP handshake, in the framing around every result, and by having no way to act on an instruction even if a model believed one.</p><h2 id="untrusted-content" tabindex="-1">Untrusted content <a class="header-anchor" href="#untrusted-content" aria-label="Permalink to “Untrusted content”">​</a></h2><p>Message bodies come back fenced between <code>BEGIN</code>/<code>END UNTRUSTED EMAIL CONTENT</code> markers carrying a random nonce, with that nonce repeated on <strong>every line</strong> inside. Text outside those markers is the only text this server wrote. The nonce is what stops a message from closing the fence itself and continuing as if it were the server talking.</p><h2 id="no-send-path" tabindex="-1">No send path <a class="header-anchor" href="#no-send-path" aria-label="Permalink to “No send path”">​</a></h2><p>There is no SMTP client here. A message that says &quot;forward this to <a href="mailto:attacker@example.com" target="_blank" rel="noreferrer">attacker@example.com</a>&quot; cannot be carried out by this server, whatever a model makes of it. That absence is what makes the framing above worth anything.</p><h2 id="writes-are-off-unless-you-say-otherwise" tabindex="-1">Writes are off unless you say otherwise <a class="header-anchor" href="#writes-are-off-unless-you-say-otherwise" aria-label="Permalink to “Writes are off unless you say otherwise”">​</a></h2><p><code>IMAP_READ_ONLY</code> defaults to <strong><code>true</code></strong>, which is the opposite of the other servers in this family and deliberate: it reaches a mailbox. The five write tools are not registered at all in that state — a model cannot ask for a tool it cannot see.</p><p><code>IMAP_ALLOW_TOOLS</code> and <code>IMAP_DENY_TOOLS</code> cut finer along the same line. A filtered tool is never registered either, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike. It covers <strong>tools</strong>; the attachment resources are not filtered.</p><h2 id="the-confirmation-honestly" tabindex="-1">The confirmation, honestly <a class="header-anchor" href="#the-confirmation-honestly" aria-label="Permalink to “The confirmation, honestly”">​</a></h2><p>Deleting messages, moving or copying them, and deleting a folder <strong>ask a person</strong> through MCP elicitation — a dialog the model cannot answer on its behalf, and which nothing proceeds without.</p><p>Where the client cannot show one, they are refused on the first call and answered with a short-lived, single-use token bound to the exact target. A model cannot mint one — it only ever exists in a previous result from this server — so an instruction hidden in a message cannot satisfy the gate. But it proves the call was made twice with the same arguments and nothing more, and the fallback text says so rather than implying somebody approved.</p><p><code>ELICITATION=false</code> moves a capable client onto that fallback deliberately, for a scheduled job or a test harness. It does not remove the guard, and the server prints one line at startup saying it is off.</p><p>See <a href="/guide/approval">Asking a person</a>.</p><h2 id="attachments" tabindex="-1">Attachments <a class="header-anchor" href="#attachments" aria-label="Permalink to “Attachments”">​</a></h2><p>Attachments are only written to disk when <code>IMAP_DOWNLOAD_DIR</code> is set, and only there: the caller does not choose the directory, so bytes from a stranger cannot land somewhere of their choosing. Types are checked against an allowlist and sizes against a cap.</p><h2 id="reading-a-document" tabindex="-1">Reading a document <a class="header-anchor" href="#reading-a-document" aria-label="Permalink to “Reading a document”">​</a></h2><p><code>mode: &quot;text&quot;</code> is the only place this server parses a binary a stranger sent — a bundled PDF.js for PDFs, a ZIP reader for Office and OpenDocument files. That is real attack surface, and it is contained rather than trusted:</p><ul><li>it runs in a <strong>child process with a heap limit and a timeout</strong>, so a parse that spins or grows is killed instead of taking the server with it. A process rather than a thread, because a thread&#39;s memory limit was measured to fail for one allocation pattern — and a process that dies dies alone;</li><li><strong>compressed streams are measured before PDF.js inflates them.</strong> A PDF stream that would decode past 32 MB, or a set of them past 128 MB, is refused as too large before any of it is materialised; ZIP entries are bounded the same way, and spreadsheet output is budgeted per row with every cell capped;</li><li>the child&#39;s <strong>stdout is discarded</strong>. The transport here is stdio JSON-RPC and PDF.js logs; one line reaching the parent&#39;s stdout would corrupt the framing;</li><li><strong>PDF.js runs with <code>isEvalSupported: false</code>.</strong> That flag gates the construction of <code>Function</code> objects from font programs in the document, which is the primitive that turned a parser bug into remote code execution in CVE-2024-4367;</li><li>only four PDF.js calls are used. Embedded JavaScript, embedded files and annotations are never asked for;</li><li>the ZIP reader decides <strong>what to decompress before the buffer is sized</strong>, from a fixed list of entry names. Nothing else in the container is read, and nothing recurses;</li><li>there is <strong>no XML parser</strong>, on purpose. The markup walk never builds an entity table and never resolves a system identifier, so billion-laughs and XXE are not defended against — they are not implemented. <code>&amp;lol9;</code> comes back as six literal characters;</li><li><strong>nothing in the path reaches the network or the filesystem.</strong> The document is passed as bytes, never as a URL, and the font and CMap paths PDF.js would otherwise fetch are deliberately left unset. Do not &quot;fix&quot; a font warning by pointing them at a CDN: it is the most-suggested workaround online and would give this server its first outbound HTTP client.</li></ul><p>Extracted text is the sender&#39;s text and goes through the same nonce fence as a message body. It also carries something a mail body does not need: extraction returns every text-drawing instruction in a file — including text set below one point, hanging off the page, or drawn in the colour of the paper — and returns nothing that was drawn as a picture. The result states that above the fence, because otherwise &quot;the document says X&quot; is a claim the reader has no way to check.</p><h2 id="reporting-a-vulnerability" tabindex="-1">Reporting a vulnerability <a class="header-anchor" href="#reporting-a-vulnerability" aria-label="Permalink to “Reporting a vulnerability”">​</a></h2><p>Use <a href="https://github.com/ni-c/imap-mcp/security/advisories/new" target="_blank" rel="noreferrer">private vulnerability reporting</a>.</p>`,25)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};